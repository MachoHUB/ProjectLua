queue_on_teleport([[
    for i = 1, 3 do
        local ok = pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/MachoHUB/ProjectLua/refs/heads/main/working"))()
        end)
        if ok then return end
        task.wait(1.5)
    end
]])

task.spawn(function()
    if not game:IsLoaded() then
        game.Loaded:Wait()
    end
    task.wait(4)
    for i = 1, 3 do
        local ok = pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/MachoHUB/machonachoscript/refs/heads/main/code"))()
        end)
        if ok then return end
        task.wait(1.5)
    end
end)
